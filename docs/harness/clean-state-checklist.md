# 乾淨狀態檢查清單

每個工作階段結束前逐項核對。有任何一項不成立，先處理完再結束。

- [ ] `./init.sh` 仍可通過（標準啟動與驗證路徑可用）。
- [ ] `claude-progress.md` 已新增本輪記錄，且「目前已驗證狀態」已更新。
- [ ] `feature_list.json` 真實反映狀態：沒有假的 `passing`，`in_progress` 最多一個。
- [ ] 沒有半完成的工作處於未記錄狀態（不是寫進 notes 就是寫進進度日誌）。
- [ ] 已 commit（或如實記錄為什麼不能 commit）。
- [ ] 下一個工作階段不需要人工修復就能直接執行 `./init.sh` 繼續。
